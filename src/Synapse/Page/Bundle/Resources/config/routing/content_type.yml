# Page content type
synapse_content_type_page:
    path: "/{path}.{_format}"
    defaults:
        _controller: "SynapsePageBundle:PageContent:render"
        _format: html
    requirements:
        path: "[^.]*"
